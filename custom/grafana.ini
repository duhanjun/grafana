# =============================================================================
# Grafana Custom Configuration
# =============================================================================
# This file contains ONLY settings that differ from defaults.ini defaults.
# It is loaded via --config flag or docker volume mount.
#
# Note: Database settings are configured via environment variables in
# custom/docker-compose.yaml for better security.
# =============================================================================

[security]
allow_embedding = true